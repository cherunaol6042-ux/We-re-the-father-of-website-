<html>
<head>
  <meta charset="utf-8">
  <title>CSS Marquee (recommended)</title>
  <style>
    .marquee {
      overflow: hidden;
      white-space: nowrap;
      background: #111;
      color: #fff;
      padding: 12px 0;
      font-family: sans-serif;
    }
    .marquee .track {
      display: inline-block;
      padding-left: 100%;
      animation: scroll-left 12s linear infinite;
    }
    @keyframes scroll-left {
      0% { transform: translateX(0%); }
      100% { transform: translateX(-100%); }
    }
  </style>
</head>
<body>
  <h2>Marquee using CSS animation</h2>
  <div class="marquee" aria-label="Scrolling announcement">
    <div class="track">This is a smooth, CSS-based marquee. Use CSS animations instead of &lt;marquee&gt; for better compatibility and accessibility.</div>
  </div>
</body>
</html>